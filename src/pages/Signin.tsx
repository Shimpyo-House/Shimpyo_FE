// 라우트가 없는 관계로 여기서 하다가 push 전에 Pay.tsx로 옮기겠습니당(커밋 기록 보시는 분들 참고 !)

import React from 'react';
import CheckBooking from '../components/layout/Pay/CheckBooking';

const Signin = () => {
  return <CheckBooking />;
};

export default Signin;
